import styled from 'styled-components';

//props:

const EntityImage = styled.img.attrs({alt: '', tabIndex: 0})`
    cursor: pointer;
    width: 100%;
    height: auto;
    border-radius: 2px;
`

export default EntityImage

